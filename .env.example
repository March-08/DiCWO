# At least one API key is required.
# Both can be set — the YAML config chooses which provider to use.

# OpenAI
OPENAI_API_KEY=sk-your-key-here

# OpenRouter (https://openrouter.ai/keys)
OPENROUTER_API_KEY=sk-or-your-key-here
